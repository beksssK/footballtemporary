const initialState = {
    allFootballers: [],
};

export const footballersReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }

};

export default footballersReducer;
